# Nawaf → Games Migration

تم توحيد أنظمة الألعاب التي كانت موزعة داخل مشروع Nawaf داخل هذا المستودع.

## البنية الحالية

- `src/index.js` — تشغيل البوت، Slash Commands، استقبال Prefix Commands وربط الأنظمة.
- `src/games-engine.js` — محرك الألعاب، الـLobby، الجلسات، الألعاب الفردية والجماعية، الإنهاء، والنقاط.
- `src/game-voting.js` — نظام `-تصويت` للفعاليات الجماعية.
- `src/draw-game.js` — فعالية الرسم والتخمين.
- `src/store.js` — SQLite، إعدادات السيرفر، النقاط، الإحصائيات، رومات الفعاليات، وروم الألعاب الفردية.

## الخصائص المنقولة

| نظام Nawaf القديم | المكان الحالي في Games |
| --- | --- |
| `games.py` | `src/games-engine.js` |
| `dice_upgrade.py` | نظام `dice` داخل المحرك |
| `event_games.py` | الألعاب الجماعية + `race` |
| `game_voting.py` | `src/game-voting.js` |
| `game_channels.py` | `src/store.js` + إعدادات رومات الألعاب |
| `game_restrictions.py` | فحوصات الصلاحيات ورومات الفعاليات داخل المحرك |
| `individual_games.py` | إعداد روم الألعاب الفردية داخل `guild_settings` |
| `mini_games.py` | الألعاب الفردية + aliases للتوافق |
| إحصائيات ونقاط الألعاب | `src/store.js` + `/نقاطي` + `/ترتيب-الألعاب` |

## أوامر مهمة

### ألعاب جماعية

`-xo` `-مافيا` `-كراسي` `-حجرة` `-نرد` `-غميضة` `-سباق` `-ريبلكا` `-دولة` `-رسمة` `-كلمة` `-روليت`

### ألعاب فردية

`.زر` `.اسرع` `.فكك` `.ادمج` `.اعلام` `.اعكس` `.حرف` `.صحح` `.ترتيب` `.الوان` `.ايموجي` `.اكشف`

### الإدارة

`-تصويت` — فتح تصويت على فعالية جماعية.

`/إعدادات-الألعاب` — رئيس الفعاليات، النقاط، الحد الأقصى، مدة الـLobby، وروم الألعاب الفردية.

`/إضافة-روم-ألعاب` و`/إزالة-روم-ألعاب` و`/رومات-الألعاب` — التحكم في رومات الفعاليات.

`/إيقاف-اللعبة` أو `-توقيف` — إيقاف اللعبة الحالية.

## التوافق

تم الاحتفاظ بـaliases قديمة من Nawaf للألعاب الفردية حتى لا تحتاج إلى تغيير كل الاختصارات القديمة دفعة واحدة.
